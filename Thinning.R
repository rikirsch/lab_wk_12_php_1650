#thinning function 

#this one is pretty simple so i will add documentation for later
thinning <- function (current_cumulated_time, arrival_rates) {
  #find which set of hours the time belongs to, store the lower value as interval
  interval<-floor(current_cumulated_time) 
  
  
}